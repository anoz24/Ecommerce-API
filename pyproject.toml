[project]
name = "ecomAPI"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]",
    "uvicorn",
    "sqlalchemy",
    "pyjwt",
    "pytest",
    "bcrypt",
]

[tool.fastapi]
entrypoint = "src.main:app"